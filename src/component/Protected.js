import React from 'react';
import PropTypes from "prop-types";


const Protected = () => {
    return (
        <div>
            Protected ...
        </div>
    );
};

Protected.propTypes = {};


export default Protected;
